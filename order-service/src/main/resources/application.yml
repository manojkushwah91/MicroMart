server:
  port: 8083

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://orderdb:3306/orderdb
    username: root
    password: Manoj@8719895574
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
